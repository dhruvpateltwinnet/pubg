/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "dl.dir.freefiremobile.com",
        ]
      },
      reactStrictMode:false
};

export default nextConfig;
